class Circle {
    constructor (x, y, size, dx, dy, speed){
        this.x = x;
        this.y = y;
        this.size = size;
        this.dx = dx;
        this.dy = dy; 
        
    }
}

// this is Ball in the GAME